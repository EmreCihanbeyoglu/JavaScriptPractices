let person = {
    firstName : "Jason",
    favColor : "blue",
    favNumber : 8,
    isMarried : true,
    children : ["child1", "child2"],
    thing: {
        name: "object",
        color: "yellow"
    }
}
console.log(person)
console.log(person.children[0])
console.log(person.thing.name)